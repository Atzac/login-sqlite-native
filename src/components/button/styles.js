import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    container: {
      width: "100%",
      marginTop: 15,
      height: 50,
      backgroundColor: "#38BE66",
      borderRadius: 5,
      marginTop: 55,
      alignItems: 'center',
      justifyContent: 'center'
    },
    title: {
      color: "#ffffff",
      fontSize: 16,
      fontWeight: 'bold'
    }
  });